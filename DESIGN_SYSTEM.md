# 威馬康健網站視覺設計規範

本文件是威馬康健網站未來視覺開發的色彩依據。新增頁面、元件、CTA、圖示與狀態樣式時，應優先使用本文件定義的語意色彩，不任意新增相近色碼。

## 核心色票

| CSS 變數 | 色碼 | 視覺定位 | 建議用途 |
| --- | --- | --- | --- |
| `--brand-primary` | `#28513d` | 主品牌色／深森林綠 | 主要 CTA、導覽狀態、深色區塊、重點圖示 |
| `--brand-accent` | `#a18548` | 主點綴色／霧面金、青銅 | 邊框、分隔線、圖示、眉題、裝飾細節 |
| `--brand-ink` | `#14291f` | 主要文字色／極深綠、墨黑 | 標題、正文、深色文字、深色頁腳 |
| `--brand-surface` | `#f7f9f8` | 淺灰綠背景 | 頁面底色、淺色區塊、表單背景 |
| `--brand-highlight` | `#f9f1a4` | 亮金黃標示色 | 標籤、數字標示、溫和提示、局部高亮 |
| `--brand-warning` | `#f1c40f` | 警示與備註色 | 警示底色、重要備註、需注意狀態 |

## 核准輔助色

| CSS 變數 | 色碼 | 視覺定位 | 建議用途 |
| --- | --- | --- | --- |
| `--brand-wine` | `#8c1821` | 酒紅行動色 | 購買、申請、活動 CTA 與少量情感重點 |
| `--brand-beige` | `#f3eadc` | 溫暖米色 | 首頁區塊分層、卡片背景、柔和內容底色 |

酒紅與米色可與六個核心色搭配，但仍屬輔助角色：酒紅不取代森林綠的主品牌定位，米色不取代淺灰綠的主要頁面底色。

## 使用原則

### 1. 主體比例

- 約 60% 使用 `--brand-surface`、白色與留白，維持乾淨、安心的醫學保健感。
- 約 30% 使用 `--brand-primary` 與 `--brand-ink` 建立品牌辨識及資訊層級。
- 約 10% 使用金色、亮黃與警示黃作為點綴，不大面積搶占內容。

### 2. 元件配色

- 主要 CTA：`--brand-primary` 背景搭配白色文字。
- 次要 CTA：透明或淺色背景、`--brand-primary` 文字、`--brand-accent` 邊框。
- 深色區塊：使用 `--brand-primary` 或 `--brand-ink`，文字以白色為主，金色僅作點綴。
- 卡片：優先使用白色卡片搭配 `--brand-surface` 頁面背景。
- 一般標籤：使用 `--brand-highlight` 背景與 `--brand-ink` 文字。
- 警示與重要備註：使用 `--brand-warning` 背景與 `--brand-ink` 文字。
- Hover、Pressed 等衍生狀態應由核心色加深或降低透明度產生，不新增另一組近似色票。

### 3. 可讀性與無障礙

- `#28513d` 在 `#f7f9f8` 上的對比約為 `8.51:1`，可用於一般文字。
- `#14291f` 在 `#f7f9f8` 上的對比約為 `14.53:1`，是正文與標題的首選。
- `#a18548` 在淺色背景上的對比不足以作為小尺寸正文，僅用於大字、粗體、圖示、邊框及裝飾。
- `#f9f1a4` 與 `#f1c40f` 不可作為白底文字色；應作為背景並搭配 `#14291f` 文字。
- 所有互動元件須保留清楚的鍵盤焦點、Hover 與 Active 狀態，不能只依賴顏色傳達意義。

## 例外色彩

- 第三方品牌識別色可保留，例如 LINE 的官方綠色，但只限對應的第三方品牌元件。
- 商品包裝、合作通路 Logo 與內容圖片可使用自身色彩，不納入網站 UI 色票。
- 純白 `#ffffff` 可用於卡片、反白文字及高對比區域。

## 舊版色彩處理

目前 `src/styles/global.css` 仍包含酒紅、米色與舊森林綠等既有色彩。這些屬於早期頁面樣式：

- 維護舊元件時可暫時保留，避免未經確認就造成全站大幅換色。
- 新頁面與新元件不得延伸使用舊酒紅或新增其他近似色。
- 若要全面換色，應另開一次全站色彩遷移工作，逐頁檢查對比、狀態與圖片搭配。

## 開發規則

1. 優先使用 `src/styles/global.css` 的 `--brand-*` 變數。
2. 不在 Astro、HTML 或元件 CSS 中直接寫入新的任意 Hex／RGB 色碼。
3. 新增色彩前先確認是否可由既有色票、透明度或 `color-mix()` 產生。
4. 完成視覺修改後，至少檢查桌機與手機版，並確認文字對比與互動狀態。
